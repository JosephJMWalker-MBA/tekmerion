name: tekmerion
description: Peace and predictability through a trustworthy record.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  crypto: ^3.0.3
  uuid: ^4.4.0
  flutter:
    sdk: flutter
  path_provider: ^2.1.6
  sqflite: ^2.4.3
  path: ^1.9.1
  file_picker: ^11.0.3
  pdfrx: ^2.4.7
  intl: ^0.20.3

  meta: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  sqflite_common_ffi: ^2.4.2

flutter:
  uses-material-design: true
